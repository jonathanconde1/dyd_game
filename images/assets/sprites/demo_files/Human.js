function Human(x,y,key,e){
    // Child of Being, parent of NPC and Player (the common aspect being the handling of speech bubbles)
    // key is a string indicating the atlas to use as texture
    Being.call(this,x,y,key,e);
}
